using UnityEngine;
using UnityEngine.UI;

public class UIManager : MonoBehaviour
{
    public GameManager gameManager;
    public Button nextStepButton;

    void Start()
    {
        nextStepButton.onClick.AddListener(gameManager.NextStep);
    }
}
